<li>
	{{burger_name}}

	<button class="change-burger" data-id="{{id}}" data-newburger ="{{devoured}}">
		{{#if devoured}}DEVOURED!{{else}}DEVOUR IT!{{/if}}
	{{!-- <button class="change-sleep" data-id="{{id}}" data-newsleep disabled="{{devoured}}">
		{{#if devoured}}DEVOURED!{{else}}DEVOUR IT!{{/if}} --}}
	</button>

	{{!-- <button class="delete-cat" data-id="{{this.id}}">DELETE!</button> --}}
</li>
